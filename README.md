# BBList

## Get Started

```bash
npm run dev
```

## Update database

After modifying or adding a database schema, run the following commands to update the remote database:

```bash
npx drizzle-kit generate
npx drizzle-kit push
```
